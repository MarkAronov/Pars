const StartPage = () => (
	<main className="min-h-screen flex items-center justify-center p-8">
		<h1 className="text-4xl font-bold tracking-tight">Welcome to Pars</h1>
	</main>
);

export default StartPage;
