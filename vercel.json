{
    "buildCommand": "cd frontend && bun install && bun run build",
    "outputDirectory": "frontend/dist",
    "installCommand": "echo 'dependencies installed per-workspace'",
    "framework": "vite"
}